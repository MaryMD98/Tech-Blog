{{#each userPosts.posts as |post|}}
    <h2 class="oneUser"><a href="/api/posts/update/{{post.id}}">{{post.title}}</a><span>{{format_date post.date}}</span></h2>
{{/each}}

    <div>
        <button class="newPOST"><a href="/api/posts/">+ New Post</a></button>
    </div>